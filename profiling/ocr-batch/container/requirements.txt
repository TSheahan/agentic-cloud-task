# Glue packages; Torch etc. often arrive transitively via docling.
# paddlepaddle-gpu is installed in the Dockerfile after this file (fat image); host poke / thin+bind may use AMI-bound Paddle instead.
docling>=2.0.0
rapidocr-paddle
boto3
pydantic
